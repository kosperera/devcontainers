version: '3.9'

services:
  app:
    container_name: app
    image: mcr.microsoft.com/vscode/devcontainers/base:dev-bullseye
        
    environment:
      ASPNETCORE_URLS: "https://+:5001;http://+:5000"
      ASPNETCORE_ENVIRONMENT: "Development"
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_NOLOGO: 1

    volumes:
      - ..:/workspace:cached
      
    # Overrides default command so things don't shut down after the process ends.
    command: sleep infinity

    # Runs app on the same network as the database container, allows "forwardPorts" in devcontainer.json function.
    network_mode: host

    # Uncomment the next line to use a non-root user for all processes.
    # user: vscode

    # Use "forwardPorts" in **devcontainer.json** to forward an app port locally. 
    # (Adding the "ports" property to this file will not forward from a Codespace.)

    # Uncomment below couple of lines for database container.
    # depends_on:
    #   - mssql-db

# Uncomment the next few lines to use MSSQL server running on a container.
# Verify there's no mssql-db container already running on port 1433.
#   mssql-db:
#       container_name: db
#       image: mcr.microsoft.com/azure-sql-edge:latest

#       environment:
#         ACCEPT_EULA: Y
#         MSSQL_SA_PASSWORD: P@ssw0rd
#         MSSQL_PID: Developer

#       ports:
#         - 1433:1433
      
#       volumes:
#         - mssql-data:/data

# volumes:
#   mssql-data:

# networks:
#   default:
#     external:
#       name: bridge